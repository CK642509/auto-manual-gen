{
  "name": "demo-stream-app",
  "version": "0.0.0",
  "private": true,
  "description": "示範用的 AI 影像串流監控台 —— 同一份前端同時以 Web 與 Electron 兩種形態執行，是整條手冊產線的靶。",
  "main": "src/main/index.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron": "npm run build && electron ."
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "electron": "^43.3.0",
    "vite": "^6.0.0"
  }
}
